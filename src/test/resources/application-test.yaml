spring.cache.type: none
spring:
  init:
    mode: always
  datasource:
    url: jdbc:postgresql://localhost:5433/jira-test
    username: '${TEST_DB_USER_NAME:jira}'
    password: '${TEST_DB_USER_PWD:JiraRush}'